export const MAX_ACTIVE_REQUESTS = 5;
export const MAX_IMAGES_PER_ENTITY = 5;
export const MAX_UPLOAD_BYTES = 10 * 1024 * 1024;
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 50;

/** Días sin actividad antes de mostrar el recordatorio "¿Seguís buscando?" */
export const REQUEST_REMINDER_DAYS = 5;
/** Días sin actividad antes de pasar a Inactiva (pierde visibilidad). */
export const REQUEST_INACTIVE_DAYS = 7;
/** Días sin actividad antes de archivar (sale de listados públicos y de vendedores). */
export const REQUEST_ARCHIVE_DAYS = 30;
