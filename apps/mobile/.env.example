# Public client config only (bundled into the app).
# Never put Stripe/JWT/DB/R2/Resend secrets here.
EXPO_PUBLIC_API_URL=https://api.buyseek.us

# Optional — same semantics as NEXT_PUBLIC_LAUNCH_COUNTRY (default US).
# EXPO_PUBLIC_LAUNCH_COUNTRY=US
